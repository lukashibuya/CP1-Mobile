import { StyleSheet } from 'react-native';
export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#FFFFFFFF',
        padding: 20,
    },
    input: {
        width: '100%',
        padding: 10,
        marginBottom: 10,
        backgroundColor: '#fff',
        borderRadius: 5,
        borderWidth: 1,
        borderColor: '#ccc',
    },
    botao: {
        backgroundColor: '#00FE11FF',
        padding: 10,
        borderRadius: 5,
        alignItems: 'center',
        width: '100%',
    },
    botaoTexto: {
        color: '#fff',
        fontSize: 20,
        fontWeight: 'bold',
    },
});
